% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{shutdown_and_reset_singleton}
\alias{shutdown_and_reset_singleton}
\title{Shut down duckplyr's singleton connection and recreate it}
\usage{
shutdown_and_reset_singleton()
}
\value{
\code{TRUE} on success, \code{FALSE} on failure.
}
\description{
Shuts down the singleton DuckDB connection to release file locks, then
recreates it so the session keeps working. The new connection gets the
same macro/R-function setup that duckplyr normally does on first access.
}
\details{
We replace \verb{$con} directly instead of setting it to NULL because duckplyr
stacks \code{reg.finalizer(onexit = TRUE)} calls that accumulate across resets.
If \verb{$con} is NULL when those finalizers fire at session exit, each one
calls \code{dbDisconnect(NULL)} and errors. Keeping a valid connection avoids
that.
}
\note{
This accesses duckplyr internals (\code{default_duckdb_connection} and
\code{create_default_duckdb_connection}). Validated against duckplyr 0.4.1.
If duckplyr changes these internals the function returns \code{FALSE} and the
caller falls back to the warning path.
}
\keyword{internal}
